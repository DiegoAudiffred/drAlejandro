
    <div id="servicesCarousel" class="carousel slide" data-ride="carousel" data-interval="false">
        <div class="carousel-inner">
            {% for doc in services %}
            {% if forloop.first %}
            <div class="carousel-item active">
                <div class="row justify-content-center">
                    {% elif forloop.counter0|divisibleby:4 %}
                </div>
            </div>
            <div class="carousel-item">
                <div class="row justify-content-center">
                    {% endif %}


                    <div class="col-6 col-md-3 mb-4 d-flex justify-content-center">
                        <div class="card w-100" style="perspective: 1000px; cursor: pointer;">
                            <div class="card-inner">
                                <!-- Frente -->
                                <div class="card-front overflow-hidden">
                                    <img src="{{ doc.image1.url }}" class="w-100 h-100 object-fit-cover">
                                    <div
                                        class="position-absolute top-50 start-50 translate-middle w-100 text-center bg-dark bg-opacity-50 py-2 fnSuperiorRegularCard text-secondary">
                                        {{ doc.name }}
                                    </div>
                                </div>
                                <!-- Reverso -->
                                <div class="card-back overflow-hidden">
                                    <img src="{{ doc.image2.url }}" class="w-100 h-100 object-fit-cover">
                                    <div
                                        class="position-absolute top-50 start-50 translate-middle text-center px-2 fnSuperiorRegularCard text-secondary">
                                        {{ doc.descripction }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    {% if forloop.last %}
                </div>
            </div>
            {% endif %}
            {% endfor %}
        </div>

        <!-- Controles afuera: botones para evitar salto de página -->
        <button class="carousel-control-prev btn-control-outside" type="button" data-target="#servicesCarousel"
            data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </button>

        <button class="carousel-control-next btn-control-outside" type="button" data-target="#servicesCarousel"
            data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </button>
    </div>
